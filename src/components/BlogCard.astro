---
interface Props {
  title: string;
  description: string;
  date: Date;
  slug: string;
  tags?: string[];
}

const { title, description, date, slug, tags = [] } = Astro.props;
const formattedDate = date.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' });
const base = import.meta.env.BASE_URL;
---

<article class="group">
  <a href={`${base}posts/${slug}`} class="block p-6 rounded-xl border border-gray-200 dark:border-slate-700 bg-white dark:bg-slate-800/50 hover:border-primary/40 dark:hover:border-primary-dark/40 hover:shadow-lg hover:shadow-primary/5 transition-all duration-300">
    <time datetime={date.toISOString()} class="text-sm text-gray-500 dark:text-gray-400">
      {formattedDate}
    </time>
    <h3 class="mt-2 text-lg font-semibold text-gray-900 dark:text-white group-hover:text-primary dark:group-hover:text-primary-dark transition-colors">
      {title}
    </h3>
    <p class="mt-2 text-gray-600 dark:text-gray-300 text-sm leading-relaxed line-clamp-2">
      {description}
    </p>
    {tags.length > 0 && (
      <div class="mt-3 flex flex-wrap gap-2">
        {tags.map(tag => (
          <span class="tag">{tag}</span>
        ))}
      </div>
    )}
  </a>
</article>
